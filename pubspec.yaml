name: appanime
description: A Mobile Application for view animes and mangás online

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  auto_size_text: ^2.1.0
  bloc: ^6.0.2
  cupertino_icons: ^1.0.0
  dartz: ^0.9.1
  flutter_bloc: ^6.0.5
  flutter_launcher_icons: ^0.8.1
  freezed_annotation: ^0.11.0+1
  get_it: ^4.0.4
  google_fonts: ^1.1.0
  intl: ^0.16.1
  material_design_icons_flutter: ^4.0.5345
  flutter_platform_widgets: ^0.72.0
  connectivity: 2.0.2
  parse_server_sdk_flutter: ^2.1.0
  equatable: ^1.2.6
  json_serializable: ^3.5.1
  http: ^0.12.2
  http_interceptor: ^0.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  #? Code generators
  build_runner: ^1.10.0

  #? Linter
  effective_dart: ^1.2.4
  freezed: ^0.11.5



flutter:
  uses-material-design: true
  
  assets:
    - assets/icons/

  